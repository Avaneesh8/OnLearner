import 'package:flutter/material.dart';

class DownloadNotes extends StatelessWidget {
  const DownloadNotes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
